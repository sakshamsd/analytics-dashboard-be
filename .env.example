# ============================================
# Analytics Dashboard Backend - Environment Variables
# ============================================

# Copy this file to .env and fill in your values
# NEVER commit .env to version control!

# --------------------------------------------
# Server Configuration
# --------------------------------------------

# Port the server will run on
PORT=4000

# Environment (development, production, test)
NODE_ENV=development

# --------------------------------------------
# Database Configuration
# --------------------------------------------

# PostgreSQL Connection URL
# Format: postgresql://username:password@host:port/database
#
# Development (local):
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/analytics_dashboard
#
# Production examples:
# Supabase: postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres
# Railway: postgresql://postgres:[PASSWORD]@[HOST].railway.app:5432/railway
# Render: postgres://[USER]:[PASSWORD]@[HOST].render.com/[DB]
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/analytics_dashboard

# --------------------------------------------
# MongoDB Configuration
# --------------------------------------------

# MongoDB Connection URI
# Format: mongodb://username:password@host:port/database
#
# Development (local):
# MONGO_URI=mongodb://localhost:27017/analytics-dashboard
#
# Production (MongoDB Atlas):
# MONGO_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/analytics-dashboard?retryWrites=true&w=majority
#
# Railway:
# MONGO_URI=mongodb://mongo:[PASSWORD]@[HOST].railway.app:27017
MONGO_URI=mongodb://localhost:27017/analytics-dashboard

# --------------------------------------------
# CORS Configuration
# --------------------------------------------

# Allowed origins for CORS (comma-separated for multiple)
# Development: Use * to allow all origins
# Production: Use your frontend URL
#
# Examples:
# CORS_ORIGIN=*
# CORS_ORIGIN=https://your-frontend.vercel.app
# CORS_ORIGIN=http://localhost:3000,http://localhost:5173
CORS_ORIGIN=*

# --------------------------------------------
# Optional: Logging Configuration
# --------------------------------------------

# Log level (error, warn, info, debug)
# LOG_LEVEL=info

# --------------------------------------------
# Optional: Rate Limiting
# --------------------------------------------

# Rate limiting is configured in code (100 req/15min per IP)
# To modify, edit src/app.ts

# --------------------------------------------
# Optional: Authentication (Future)
# --------------------------------------------

# JWT Secret (uncomment when implementing auth)
# JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# JWT Expiration (e.g., 1h, 7d, 30d)
# JWT_EXPIRATION=7d

# Auth0 Configuration (if using Auth0)
# AUTH0_DOMAIN=your-domain.auth0.com
# AUTH0_AUDIENCE=https://your-api-audience
# AUTH0_CLIENT_ID=your-client-id
# AUTH0_CLIENT_SECRET=your-client-secret

# --------------------------------------------
# Optional: External Services (Future)
# --------------------------------------------

# Email Service (SendGrid, AWS SES, etc.)
# EMAIL_FROM=noreply@yourdomain.com
# SENDGRID_API_KEY=your-sendgrid-key

# File Storage (AWS S3, Cloudinary, etc.)
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_REGION=us-east-1
# AWS_S3_BUCKET=your-bucket-name

# Error Tracking (Sentry)
# SENTRY_DSN=https://xxxxx@sentry.io/xxxxx

# --------------------------------------------
# Optional: Feature Flags
# --------------------------------------------

# Enable/disable features
# ENABLE_SWAGGER_DOCS=true
# ENABLE_REQUEST_LOGGING=true

# ============================================
# IMPORTANT SECURITY NOTES:
# ============================================
#
# 1. NEVER commit .env file to git
# 2. Use strong passwords for production databases
# 3. Rotate secrets regularly
# 4. Use platform-specific secret management in production
#    - Render: Dashboard → Environment
#    - Railway: Dashboard → Variables
#    - Fly.io: flyctl secrets set
# 5. Set CORS_ORIGIN to specific domains in production
# 6. Enable SSL/TLS for database connections in production
#
# ============================================
